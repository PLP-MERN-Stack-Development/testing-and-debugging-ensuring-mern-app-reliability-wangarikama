// server/tests/setup.js

// This file is required by jest.config.js but can be left empty
// if your tests handle their own setup/teardown (like posts.test.js does).


// If you needed to set global environment variables or connect to a non-in-memory DB 

// Example: setting a global timeout for database operations
// jest.setTimeout(30000); 

// module.exports = async () => { /* ... */ };